language: python
python:
  - "3.7"
  - "3.8"
  - "3.9-dev"
install:
  - pip install -r requirements.txt
script:
  - python -m unittest discover
notifications:
      email:
        on_success: never
        on_failure: always